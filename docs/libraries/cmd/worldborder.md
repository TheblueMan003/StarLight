## lazy void cmd.worldborder.set(int $size, int $time)
- int $size
- int $time

Set World Border `size` in Block with `time`

## lazy void cmd.worldborder.add(int $size, int $time)
- int $size
- int $time

Add World Border `size` in Block with `time`

## lazy void cmd.worldborder.center(int $x, int $y, int $z)
- int $x
- int $y
- int $z

Set World Border Center `x` `z`

## lazy void cmd.worldborder.center(int $x, int $z)
- int $x
- int $z

Set World Border Center to `x` `z`

## lazy void cmd.worldborder.center()
Set World Border Center

## lazy void cmd.worldborder.buffer(int $size)
- int $size

Set World Border Buffer Zone to `size`

## lazy void cmd.worldborder.damage(int $dmg)
- int $dmg

Set World Border Damage Amount per block to `dmg`

## lazy void cmd.worldborder.warningtime(int $time)
- int $time

Set World Border Warning `time`

## lazy void cmd.worldborder.warningdistance(int $dist)
- int $dist

Set World Border Warning Distance to `dist`


