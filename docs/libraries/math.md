## int math.abs(int x)
- int x

Return the absolute value of x

## float math.abs(float x)
- float x

Return the absolute value of x

## int math.max(int a, int b)
- int a
- int b

Return the max between the a and b

## float math.max(float a, float b)
- float a
- float b

Return the max between the a and b

## int math.min(int a, int b)
- int a
- int b

Return the min between the a and b

## float math.min(float a, float b)
- float a
- float b

Return the min between the a and b

## bool math.isClose(float x, float y, float maxDiff)
- float x
- float y
- float maxDiff

return true if difference between x and y smaller than maxDiff

## float math.sqrt(float value)
- float value

return squart root of value

## float math.root(float n)
- float n



## int math.factorial(int x)
- int x

return x!

## float math.pow(float x, int n, float m)
- float x
- int n
- float m

return x^n

## int math.pow(int x, int n, int m)
- int x
- int n
- int m

return x^n

## int math.sign(float value)
- float value

Return sign of the argument-1 if value smaller than 01 otherwise

## int math.sign(int value)
- int value

Return sign of the argument-1 if value smaller than 01 otherwise

## float math.round(float value)
- float value

Round float to closest value

## float math.floor(float value)
- float value

Round float to lowest value

## float math.ceil(float value)
- float value

Round float to upper value

## int math.clamp(int value, int a, int b)
- int value
- int a
- int b

Clamp the value between a and b

## float math.clamp(float value, float a, float b)
- float value
- float a
- float b

Clamp the value between a and b

## (int,int) math.sorted(int a, int b)
- int a
- int b

Return (a,b) if a <= b else return (b,a)

## (float,float) math.sorted(float a, float b)
- float a
- float b

Return (a,b) if a <= b else return (b,a)

## float math.linearLerp(float a0, float a1, float w)
- float a0
- float a1
- float w

Return the linear interpolated value of a0 and a1 with coefficient w

## float math.smoothLerp(float a0, float a1, float w)
- float a0
- float a1
- float w

Return the 3rd degree interpolated value of a0 and a1 with coefficient w

## float math.sin(float angle)
- float angle

Compute the sinus of the angle in degrees with a taylor series

## float math.cos(float angle)
- float angle

Compute the cossinus of the angle in degrees with a taylor series

## float math.tan(float angle)
- float angle

Compute the tangente of the angle in degrees with a taylor series

## float math.asin(float angle)
- float angle

Compute the arcsinus of the angle in degrees with a taylor series

## float math.acos(float angle)
- float angle

Compute the arccossinus of the angle in degrees with a taylor series

## float math.atan(float angle)
- float angle

Compute the arctangente of the angle in degrees with a taylor series

## float math.atan2(float y, float x)
- float y
- float x

Compute the arctangente of the angle in degrees with a taylor series

## float math.sqrt2(float value)
- float value

Compute the square root of the value with a taylor series

## float math.log(float x)
- float x

Compute the log of x with a taylor series

## float math.exp(float x)
- float x

Compute the exp of x with a taylor series

## float math.pow(float x, float y)
- float x
- float y

Compute the pow of x with a taylor series


